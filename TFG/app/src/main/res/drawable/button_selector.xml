<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el botón está presionado -->
    <item android:state_pressed="true" android:drawable="@drawable/background_pressed" />
    <!-- Estado cuando el botón está enfocado
    <item android:state_active="true" android:drawable="@drawable/background_focused" /> -->
    <!-- Por defecto, el estado normal -->
    <item android:drawable="@android:color/transparent" />
</selector>